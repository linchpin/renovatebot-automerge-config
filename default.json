{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "DEPRECATED. This preset has moved to github>linchpin/renovatebot-config:automerge and now forwards to it so existing consumers keep working unchanged. Point new and existing projects at the new preset; this repository will be sunset once nothing extends it.",
  "extends": [
    "github>linchpin/renovatebot-config:automerge"
  ]
}
